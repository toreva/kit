{
  "name": "@toreva/sdk",
  "version": "0.2.0",
  "description": "MCP-native Solana primitive platform. R1 (today): read-only Kamino + Marginfi APY compare and SPL token receive detection. R2-R5: family-by-family rollout. See toreva.com/operational-matrix for current state.",
  "keywords": [
    "solana", "defi", "kamino", "marginfi", "spl-token",
    "non-custodial", "mcp", "agent-execution", "agent-skills",
    "read-only", "discovery", "earn", "lending", "apy", "sdk"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/toreva/kit"
  },
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@toreva/types": "workspace:*"
  }
}
