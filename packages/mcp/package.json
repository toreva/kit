{
  "name": "@toreva/mcp",
  "version": "0.2.0",
  "mcpName": "io.github.toreva/kit",
  "description": "Policy-checked Solana wallet actions. Users set the limits. Toreva checks, executes only when allowed, explains, and receipts every outcome.",
  "keywords": [
    "solana",
    "wallet-policy",
    "receipts",
    "delegation",
    "non-custodial",
    "execution-primitives",
    "mcp",
    "agent"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/toreva/kit"
  },
  "license": "MIT",
  "type": "module",
  "bin": {
    "toreva-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./bus-publisher": {
      "types": "./dist/bus-publisher.d.ts",
      "default": "./dist/bus-publisher.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@toreva/types": "workspace:^",
    "zod": "^3.24.2",
    "zod-to-json-schema": "^3.25.2"
  }
}
