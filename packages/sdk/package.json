{
  "name": "@toreva/sdk",
  "version": "0.2.1",
  "description": "TypeScript client for Toreva gateway relay and governed-object connectors.",
  "keywords": [
    "gateway", "governed-objects", "receipts", "relay",
    "compute-binding", "mcp", "agent", "sdk"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/toreva/kit"
  },
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@toreva/types": "workspace:*"
  }
}
